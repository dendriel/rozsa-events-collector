spring:
  h2:
  sql.init.mode: always
  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true

logging:
  level:
    com:
      rozsa:
        events:
          collector: INFO